<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">Notebook</a>
    </div>
    <div id="navbar" class="collapse navbar-collapse"> 
        <ul  class="nav navbar-nav">
          <% if (!isAuthenticated) { %>
            <li class="main-header__item">
                <a class="<%= path === '/login' ? 'active' : '' %>" href="/login">Login</a>
            </li>
            <li class="main-header__item">
                <a class="<%= path === '/signup' ? 'active' : '' %>" href="/signup">Signup</a>
            </li>
        <% } else { %>
          <ul class="nav navbar-nav">
            <li class="<%= path === '/' ? 'active' : '' %>">
              <a href="/">Notes</a>
            </li>
    
            <li class="<%= path === '/add-note' ? 'active' : '' %>">
              <a href="/add-note">Add Note</a>
            </li>
    
            <!-- <li class="<%= path === '/manage-notes' ? 'active' : '' %>">
              <a href="/admin/manage-notes">Manage Notes</a>
            </li>  -->
          <li>
            <form action = '/logout' method = 'post' class="navbar-brand" >
              <button type="submit" class="unstyled-button">Logout</button>
            </form>
          </li>
          <% } %>
        </ul>
      </ul>
    </div>
    <!--/.nav-collapse -->
  </div>
</nav>